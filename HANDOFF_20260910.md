# HiveRobot handoff — 2026-09-10

## Current state

Work since Git commit `55cbb62` (2026-09-06) is recorded in this commit. The active integrated entry point is
`scripts/chessboard_vision_v2_8_web_control.py`. It combines the v2.7 chessboard vision page, the v2.6 arm page and
the v2.8 crown-geometry teaching page while preserving the legacy data paths and file formats.

Start on the Orin from `/home/nvidia/hive_robot/DM_Control_Python`:

```bash
python3 chessboard_vision_v2_8_web_control.py --host 0.0.0.0 --port 8098 --enable-execute
```

Open `http://192.168.0.199:8098/`. Do not run the old v2.6 arm Web controller at the same time because the two
processes do not share an action lock.

The most recently reported YOLO model is:

```text
/home/nvidia/hive_robot/runs/chess_piece_yolo/yolo11n_rank4-13/weights/best.pt
```

The model weights and the full image datasets remain on the Orin and are intentionally not duplicated in this
Windows Git checkout.

## Work completed since 2026-09-06

### Chess vision and prediction display

- Prediction images now draw the selected per-square YOLO result, bounding box, class and confidence.
- Inspect can display the yellow board grid and green YOLO box together.
- Full-board crown projection maps a detection by the lower part of its box so tall pieces such as kings do not
  create a false occupied square one rank behind them.
- Inspect no longer requires a depth detection before running YOLO. YOLO can recover a piece missed by the depth
  geometry stage while retaining the original geometry result in `geometry_detection`.
- Height-assisted results retain the original RGB class and confidence semantics in their output and overlay.

### Height sampling and height-assisted identity

- Added RGB/depth height sample capture for all 12 chess-piece classes with train/val/test splits.
- Added metric board-plane calibration, sample validation, model building and test reporting.
- A height class is activated only after valid train/val samples and ruler-error checks pass. Test samples never fit
  the profile.
- Live height assistance abstains unless both the original and alternative classes have validated profiles and the
  measured height supports one unambiguous alternative.
- HP60C measurements observed during this session were not metrically accurate enough for absolute classification:
  a 40 mm pawn was commonly estimated near 8–10 mm and a bishop near 26 mm. Do not force-enable these profiles.
  Relative-height logic is promising but still experimental.
- An invalid wide object temporarily labeled `black_pawn` was removed from the intended dataset and must not be
  treated as a real chess-piece height sample.

### v2.8 integrated Web controller and arm geometry

- Added a new v2.8 file rather than modifying the v2.6 arm controller.
- Preserved vision calibration, empty-board baselines, YOLO datasets/models, arm saved moves and Web logs.
- Added a crown-geometry teaching page and a non-executable pose preview. It does not yet run IK or autonomous grasp.
- Recorded the modified arm link measurements, qualitative positive joint directions, board reference points and
  bishop crown grip measurements in `scripts/modified_arm_measurements_v2_8.json`.
- The provisional board-to-arm transform fails the current 3 mm check: held-out a8/h8 residuals are about 12.0 mm
  and 11.4 mm. It remains `validated=false` and `executable=false`.

### Home, clearance and saved-move self-learning

- `scripts/left_arm_v2_8.py` wraps v2.6 Home and clearance. Home now forces every selected joint through the formal
  return after clearance, then performs one bounded verification pass from fresh feedback.
- Captured Home/clearance transitions receive a 5 degree limit margin only when the recorded endpoint is within that
  margin. Larger transitions are still rejected.
- Clearance learns a pose-local residual and validates participating joints at 0.5 degrees. The legacy 180 degree
  `shoulder_rotate` clearance deadband remains excluded from validation.
- `scripts/left_arm_v2_8_move_library.py` inherits the v2.6 shared adaptive parameters, then adds local corrections
  matched by seven-joint target-pose distance. Large pose changes create a new anchor.
- Before the final wrist motion, all six other joints must pass the tolerance. Failed checks save residuals and stop
  without an extra correction jerk.
- Wrist-induced hold errors learn once per completed wrist motion with bounded integral updates. The final seven-joint
  snapshot determines `training` or `validated` for each move.
- The nominal tolerance is 0.5 degrees. A 0.011 degree half-encoder-count comparison margin prevents an identical
  0.5027088 degree feedback bin from blocking forever; the effective acceptance edge is 0.511 degrees.

## bishop01 reference and validation

`bishop01` was overwritten on 2026-09-10 19:20:31 with this saved pose:

| Joint | Target (rad) |
|---|---:|
| shoulder_front | 0.744449556 |
| shoulder_side | -2.776188374 |
| shoulder_rotate | 2.702944994 |
| elbow | 0.392729074 |
| arm_roll | -2.773899555 |
| wrist_side | 0.167658508 |
| wrist | -2.609864950 |

The session used one Capture and seven Home → Replay cycles. Total Replay runtime was 452.832 seconds. Runs 1–4
trained the pre-wrist pose; runs 5–7 trained wrist-induced holding. Run 7 returned code 0 and recorded `validated`.

Final signed errors:

| Joint | Error (deg) |
|---|---:|
| shoulder_front | -0.174853 |
| shoulder_side | -0.218580 |
| shoulder_rotate | 0.480859 |
| elbow | 0.152998 |
| arm_roll | -0.131153 |
| wrist_side | 0.109285 |
| wrist | 0.240423 |

The maximum absolute error was 0.480859 degrees. Full per-run values are in
`scripts/data/bishop01_replay_training_20260910.json`; the table and charts are in
`outputs/bishop01_training_20260910/bishop01_training_report_20260910.xlsx`.

## Data that must remain on the Orin

Do not delete, regenerate or overwrite these while deploying code:

```text
/home/nvidia/hive_robot/DM_Control_Python/data/left_arm_v2_8_local_target_bias.json
/home/nvidia/hive_robot/DM_Control_Python/data/left_arm_v2_8_clearance_bias.json
/home/nvidia/hive_robot/DM_Control_Python/data/left_arm_v2_table_clearance.json
/home/nvidia/hive_robot/DM_Control_Python/data/left_arm_v2_home.json
/home/nvidia/hive_robot/DM_Control_Python/data/left_arm_v2_6_saved_moves.json
/home/nvidia/hive_robot/datasets/chess_pieces_yolo/
/home/nvidia/hive_robot/runs/chess_piece_yolo/
```

Also preserve every v2.6 adaptive-tuning JSON referenced by `left_arm_v2_6.py`; v2.8 uses those values as its shared
baseline. The authoritative v2.8 bias files could not be copied into this commit because SSH access from the Windows
machine was rejected. The supplied logs were therefore converted into the committed bishop01 session JSON. At the
start of the next session, back up the two v2.8 bias files before changing any replay-learning code.

## Verification completed

- Python syntax compilation for all changed/new Python files.
- Unit tests for height sampling/model behavior, YOLO fusion, v2.8 integration, crown geometry, Home and local replay
  bias.
- Spreadsheet formulas, chart bindings and rendered layouts were checked; no formula errors were found.
- Real Orin validation: clearance reported `validated`; bishop01 Replay run 7 reported `validated` with return code 0.

## 2026-09-11 Replay 后夹爪选项

- v2.8 的机械臂页面在 Replay 按钮旁增加了 `Replay 后合拢夹爪` 复选框，默认不勾选。
- 勾选后，浏览器等待所选保存动作的 Replay 进程结束，再调用原有 `/api/action/claw-close`；夹爪继续使用
  v2.6 已有的压力/堵转检测、回退和保持逻辑。
- 此选项只按流程先后顺序触发，不增加新的关节到位验证，也不根据 Replay 返回码筛选。
- Home、clearance 和独立的 Claw Close 均未改变；claw 没有加入 Home 关节集合。

## 2026-09-11 Placement1：bishop01 到 Clearance

- v2.8 机械臂页面新增 `Placement1：夹取后到 Clearance` 复选框，目前只接受保存动作 `bishop01`。
- 勾选后，`bishop01` Replay、压力停止夹取和搬运到 Clearance 在同一个
  `left_arm_v2_8_move_library.py replay-move --placement1` 进程内执行。
- 在夹爪闭合期间持续向七个手臂关节发送保持命令；搬运时同时保持 claw 和被 clearance 规则排除的
  `shoulder_rotate`，避免两个阶段交接时失去保持力。
- Placement1 使用已保存的 Clearance 目标和 `left_arm_v2_8_clearance_bias.json` 局部学习记录；结束后按
  0.5 度名义阈值（含原有半编码器计数比较余量）验证并学习残差。
- 原有 Replay、独立 Claw Close、Clearance 和 Home 路径没有改变。当前只完成 Placement1；从 Clearance
  到开局落子位置的 Placement2 尚未实现。

### 首次实机异常及修正

- 2026-09-11 14:48 的首次实机运行在夹爪阶段报告 `contact:false`，随后手臂严重抖动、下坠；最终
  `wrist_side` 和 `wrist` 分别偏离约 8.70°、89.37°。
- 原因是夹爪阶段错误使用 Clearance 静态保持力矩，且进入搬运时 elbow 等关节的 bishop01 保持力矩
  被瞬间撤掉。失败运行产生的 Placement1 clearance 学习值不可信。
- 修正后从 bishop01 最终 wrist 动作直接继承实际使用的保持增益和力矩，并在搬运前 35% 行程内平滑
  过渡到 Clearance 运动力矩；夹爪闭合期间持续保持全部手臂关节。
- `contact:false` 现在会停止在原位，不再开始搬运；耦合阶段后恢复现有 wrist 精调。
- 大于 10° 的系统性运动误差不再进入学习。下一次 v2.8 Clearance/Placement1 会自动回滚上述失败运行
  写入的 `arm_roll`、`wrist_side`、`wrist` 偏差，再使用失败前的数值。
- 修复 Claw Home 无法接管的问题：早期 Placement1 的运行状态名为 `replay-move:bishop01`，而 v2.6
  Claw Home 只会中止 `claw-close`，因此会被活动任务拒绝。新流程登记为 `placement1:bishop01`；v2.8
  Claw Home 可中止 `claw-close`、新 Placement1 及旧状态名，然后立即执行原有 claw-home 命令。

## Next work

1. Back up the authoritative Orin v2.8 local and clearance bias JSON files.
2. Run one later cold-start confirmation of `Home → bishop01` to measure repeatability without continuing training
   unless a joint exceeds the gate.
3. Complete joint-axis vectors, motor zero offsets, direction signs, limits and TCP offset calibration.
4. Add enough independent board-to-arm reference points to bring held-out error below the chosen motion tolerance.
5. Implement IK and collision checks only after the transform and kinematic chain are validated.
6. Connect recognized D4 pieces to a dry-run grasp plan, then validate crown approach and retreat before enabling
   automatic execution.

## 2026-09-11 Placement1 learning isolation and recovery

- Root cause confirmed: the first Placement1 implementation wrote payload-carry errors into the ordinary Table Clearance `hold_bias["clearance"]` records.
- Placement1 now reads the restored shared Clearance bias as a baseline and writes all new carry-specific learning only to `data/left_arm_v2_8_placement1_clearance_bias.json`.
- A one-time migration restores the affected shared anchor `8b20a284d5592a0c` to the values printed immediately before the first Placement1 run: `arm_roll=0.3671909698 deg`, `wrist_side=0.6578945784 deg`, `wrist=-0.8207468246 deg`.
- Placement1 errors above 5 degrees are recorded for diagnosis but cannot update learned bias.
- On the first Table Clearance or Placement1 run after deployment, look for `v2.8 shared clearance Placement1 contamination recovery=` in stdout.
- Recovery migration v2 exits immediately after writing the historical values. It does not open the motor controller or execute Clearance, preventing the same run from overwriting the restored values.
- A dedicated `Restore Pre-Placement1 Clearance Data` web action now force-restores the historical values on every request and never enters a motor command, independent of migration markers.
- Ordinary Clearance now snapshots the active restored bias, never updates active bias after a failed validation, and performs up to two bounded 6-second `wrist_side` residual corrections while holding all other joints. A failure remains diagnostic only; successful verification is required before any learned update.
- Explicit restore also creates `left_arm_v2_8_clearance_bias.json.pre_placement1_restored.json` once as an external backup.

### 2026-09-11 Clearance residual fine correction

- The 15:53 Table Clearance run used the first residual-correction implementation (`wrist_side kp=16.0`, `kd=2.2`). It reduced the `wrist_side` error from 1.7267 degrees to 1.2458 degrees, but the generic hold feed-forward torques let `elbow` and `wrist` drift to 2.8851 and 1.4863 degrees.
- Transactional validation worked: this failed run did not update the active shared Clearance bias and retained the pre-run best snapshot.
- Residual correction now captures every joint's live position and measured load torque immediately before correction. It holds all six non-active joints, including `shoulder_rotate`, at those live positions with the captured torque while correcting `wrist_side`.
- The bounded `wrist_side` residual-correction gains are now `kp=24.0`, `kd=3.0`. New logs include `inherited_hold_tau`; its presence confirms the revised path is running.
- Real Orin validation at 2026-09-11 15:57 confirmed the revised path. `wrist_side` residual error fell from 1.4863 to 0.4153 degrees while the live-load holds prevented the earlier elbow and wrist drop. Final signed errors were `shoulder_front=-0.3716`, `shoulder_side=0.4153`, `elbow=0.2404`, `arm_roll=0.2841`, `wrist_side=0.4153`, and `wrist=-0.1530` degrees; the command returned 0 with status `validated`.
- An immediate bishop01 Replay at 16:00 repeated the pre-step Clearance. All held joints remained stable, but first-pass `wrist_side` residual varied to 0.6120 degrees and correctly blocked Replay without updating active learning data. Clearance residual correction now allows at most two passes: a second pass runs only when the freshly measured first-pass error remains outside 0.511 degrees, and it recaptures live positions and torques before moving.
- At 16:04, a second position-only pass changed `wrist_side` residual from 0.6120 to 0.6339 degrees. The second pass now commands the nominal position with the just-measured active-jointe load torque as bounded feed-forward (`abs(tau) <= 1.0`) instead of repeating target over-bias.
- At 19:17 and 19:21, pre-step Clearance validated at 0.3934 and 0.4371 degrees. bishop01 then reached its pre-wrist gate but missed on `wrist_side` by -0.5246 and -0.7431 degrees. The latter run correctly identified degradation, but the old backoff both restored the best bias and took another exploratory step from the same degraded observation.
- Pose-local backoff now restores the proven `best_bias_deg` exactly for that run and records the actual applied bias in `previous_bias_deg`. Exploration resumes only after a fresh replay measurement.
- The 19:24 run started before the corrected backoff was deployed. Clearance validated at 0.4590 degrees, but bishop01 `wrist_side` reached -1.2021 degrees and the old code wrote another malformed backoff bias of -0.1202 degrees. On load, v2.8 now detects the old signature (`learning_state=backoff`, logged previous equals best, active bias differs from best), restores the saved best bias automatically, and logs `v2.8 legacy local backoff recovery=` before continuing Replay.
- At 19:27 the recovery applied and bishop01 used zero `wrist_side` position bias, but its pre-wrist error repeated at -1.2021 degrees. The adaptive group log showed load feed-forward only for `arm_roll`; `wrist_side` was relying on standing position error to support its load.
- Low-shoulder adaptive Replay group-2 now applies the existing calibrated `REPLAY_LOW_SHOULDER_WRIST_SIDE_HOLD_TAU=-0.35` as active `wrist_side` feed-forward. A one-time pose-local migration resets the obsolete `wrist_side` position bias learned under zero active torque, preserving its record and sample history. Clearance, Home and final wrist control are unchanged.
- The 19:32 run was still executing the earlier Jetson process: its group-2 `active_tau` contained only `arm_roll`, it applied the old -0.1202-degree position bias, and no active-tau baseline-reset log appeared. Replay startup now prints build tag `v2.8-replay-wrist-side-active-tau-v1` so deployment can be verified directly from stdout.
- The 16:04 two-pass trial showed that repeating position-only compensation did not remove static load error: `wrist_side` changed from 0.6120 to 0.6339 degrees. The second pass now commands the nominal position with the freshly observed active-joint torque as load feed-forward, limited to an absolute value of 1.0; the first pass remains position-only.
- At 19:17 the pre-step Clearance validated with `wrist_side=0.3934` degrees and bishop01 Replay proceeded. The pre-wrist gate then stopped on only `wrist_side=-0.5246` degrees, 0.0136 degrees beyond the effective gate, and updated the isolated bishop01 local bias from -0.4317 to -0.5156 degrees for the next smooth Replay. The final wrist motion was not issued.
- In the 20:06 Placement1 run, bishop01 and final wrist both validated, but wrist dropped during claw closing and no pressure contact was detected. The final wrist controller was supporting the joint with a nonzero target error while `active_tau=0`; Placement1 replaced that controller target with the measured wrist position, instantly removing its supporting PD torque. Placement1 now inherits the exact final-wrist targets, gains, and feed-forward values for every arm joint through the claw-close phase. The takeover log reports `carry_hold_source=final_wrist_controller`.
- In the 20:15 run, the seamless claw takeover worked and pressure contact was detected. The former Placement1 then moved all six clearance-controlled joints and wrist simultaneously; it also excluded `shoulder_rotate` from both motion and verification due to the ordinary Clearance 180-degree deadband. Consequently, the arm could sweep across occupied squares and visually remain far from the captured Clearance even though the log blocked only on `wrist=2.4917` degrees. `shoulder_rotate` was approximately 43.7 degrees from its captured Clearance value but was not reported.
- Placement1 now retracts wrist to its learned Clearance command first while maintaining the exact final-wrist holds and claw pressure. It applies up to two wrist residual passes against the nominal captured Clearance and stops before sweeping the arm if wrist is still outside 0.511 degrees. The following coupled phase moves all other six joints, including `shoulder_rotate`, while holding wrist and claw. Final output logs nominal Clearance, learned command, actual position, and signed error for all seven joints. Ordinary Table Clearance keeps its existing rotate-skip behavior.
### 2026-09-11 20:29 Placement1 末端抖动修正

- 实机日志确认 wrist 先行回撤正常，回撤误差最终为 `-0.066°`。
- 六关节联动结束时，旧代码立即切换控制参数：`shoulder_rotate` 的 `kp` 从 24 跳到 45、`arm_roll` 从 18 跳到 26，同时 `shoulder_front` 前馈力矩从 1.0 跳到 3.0。此时关节仍有跟踪误差，参数突变会造成终点冲击和抖动。
- Placement1 收尾现保持轨迹终点相同的增益和前馈力矩 1.5 秒，再读取七关节最终位置；不会在终点突然换成 Clearance 的另一套保持参数。
- 新构建标识：`v2.8-placement1-smooth-terminal-v1`。关键日志：`v2.8 placement1 smooth terminal settle=`。
- 本次抖动运行最终 Clearance 误差为：shoulder_front `-0.372°`、shoulder_side `0.809°`、shoulder_rotate `-2.142°`、elbow `-0.022°`、arm_roll `-2.907°`、wrist_side `1.399°`、wrist `0.219°`。Placement1 独立学习文件已记录四个超差关节，未写入共享 Clearance 学习数据。

### 2026-09-11 20:43 Placement1 学习回退

- 平滑收尾连续两轮没有出现抖动。第二轮相较第一轮，shoulder_front、shoulder_side、elbow、shoulder_rotate 均改善，arm_roll 与 wrist 已进入 `0.5°`；wrist_side 在指令增加 `0.4°` 后反而由 `3.453°` 恶化至 `3.650°`。
- 原持续积分型 hold-bias 学习器只在误差变号时减小步长，不会在同方向误差明显恶化时回退。Placement1 现启用 improvement-aware backoff：改善时继续学习、近似持平时继续积分、恶化超过 `0.15°` 时恢复已知较好偏置并减半步长。
- 已知的 wrist_side `1.2°` 恶化步骤会在下一次运行前自动恢复到 `0.8°`，其他关节已取得的改善保持不变。新构建标识：`v2.8-placement1-adaptive-backoff-v1`。
- 20:49 运行确认恢复执行，但恢复记录未降低步长；`0.8°` 的重复结果仅改善 `0.022°`，学习器因此再次提出已拒绝的 `1.2°`。现增加 rejected-step memory：再次遇到该状态时恢复 `0.8°` 并将步长缩为 0.5，下一次最多探索到 `1.0°`。新构建标识：`v2.8-placement1-rejected-step-memory-v1`。
- 21:27 replay 在前置 Table Clearance 阶段提前结束，尚未进入 bishop01。除 wrist_side 外的受检关节均在 `0.5°` 内；wrist_side 两轮残差修正由 `0.678°` 改善到 `0.656°`、再到 `0.612°`，仅比有效门限 `0.511°` 多 `0.101°`。Clearance wrist_side 现允许第三次受限负载前馈修正，继续使用最新测得力矩并保持 `±1.0` 上限。
- 用户随后明确要求 Clearance 可以持续修正，但到位与否不得阻止下一步。前置 Clearance 现为 best-effort：仍执行严格测量及最多三轮受限修正，保留误差、验证状态与最佳快照；若仍超过 `0.5°`，输出 `v2.8 clearance residual accepted for follow-up=` 并以成功状态继续 bishop01。运动前最大角度限制继续生效。构建标识：`v2.8-clearance-nonblocking-v1`。
- 21:33 完整流程已越过前置 Clearance 并进入 Placement1；Placement1 最终五个关节到位，只剩 shoulder_rotate `-1.836°`、wrist_side `2.951°`，但旧的 Placement1 最终验证仍返回错误。现将同一 nonblocking 规则应用到 Placement1：残差继续记录及学习，返回成功供 Placement2 使用。wrist 预回撤在误差不超过 `5°` 时也记录后继续；超过 `5°` 仍视为运动故障，保留防止未抬爪便横扫棋盘的安全停止。构建标识：`v2.8-clearance-nonblocking-full-v1`。
