# HiveRobot handoff — 2026-09-10

## Current state

Work since Git commit `55cbb62` (2026-09-06) is recorded in this commit. The active integrated entry point is
`scripts/chessboard_vision_v2_8_web_control.py`. It combines the v2.7 chessboard vision page, the v2.6 arm page and
the v2.8 crown-geometry teaching page while preserving the legacy data paths and file formats.

Start on the Orin from `/home/nvidia/hive_robot/DM_Control_Python`:

```bash
python3 chessboard_vision_v2_8_web_control.py --host 0.0.0.0 --port 8098 --enable-execute
```

Open `http://192.168.0.199:8098/`. Do not run the old v2.6 arm Web controller at the same time because the two
processes do not share an action lock.

The most recently reported YOLO model is:

```text
/home/nvidia/hive_robot/runs/chess_piece_yolo/yolo11n_rank4-13/weights/best.pt
```

The model weights and the full image datasets remain on the Orin and are intentionally not duplicated in this
Windows Git checkout.

## Work completed since 2026-09-06

### Chess vision and prediction display

- Prediction images now draw the selected per-square YOLO result, bounding box, class and confidence.
- Inspect can display the yellow board grid and green YOLO box together.
- Full-board crown projection maps a detection by the lower part of its box so tall pieces such as kings do not
  create a false occupied square one rank behind them.
- Inspect no longer requires a depth detection before running YOLO. YOLO can recover a piece missed by the depth
  geometry stage while retaining the original geometry result in `geometry_detection`.
- Height-assisted results retain the original RGB class and confidence semantics in their output and overlay.

### Height sampling and height-assisted identity

- Added RGB/depth height sample capture for all 12 chess-piece classes with train/val/test splits.
- Added metric board-plane calibration, sample validation, model building and test reporting.
- A height class is activated only after valid train/val samples and ruler-error checks pass. Test samples never fit
  the profile.
- Live height assistance abstains unless both the original and alternative classes have validated profiles and the
  measured height supports one unambiguous alternative.
- HP60C measurements observed during this session were not metrically accurate enough for absolute classification:
  a 40 mm pawn was commonly estimated near 8–10 mm and a bishop near 26 mm. Do not force-enable these profiles.
  Relative-height logic is promising but still experimental.
- An invalid wide object temporarily labeled `black_pawn` was removed from the intended dataset and must not be
  treated as a real chess-piece height sample.

### v2.8 integrated Web controller and arm geometry

- Added a new v2.8 file rather than modifying the v2.6 arm controller.
- Preserved vision calibration, empty-board baselines, YOLO datasets/models, arm saved moves and Web logs.
- Added a crown-geometry teaching page and a non-executable pose preview. It does not yet run IK or autonomous grasp.
- Recorded the modified arm link measurements, qualitative positive joint directions, board reference points and
  bishop crown grip measurements in `scripts/modified_arm_measurements_v2_8.json`.
- The provisional board-to-arm transform fails the current 3 mm check: held-out a8/h8 residuals are about 12.0 mm
  and 11.4 mm. It remains `validated=false` and `executable=false`.

### Home, clearance and saved-move self-learning

- `scripts/left_arm_v2_8.py` wraps v2.6 Home and clearance. Home now forces every selected joint through the formal
  return after clearance, then performs one bounded verification pass from fresh feedback.
- Captured Home/clearance transitions receive a 5 degree limit margin only when the recorded endpoint is within that
  margin. Larger transitions are still rejected.
- Clearance learns a pose-local residual and validates participating joints at 0.5 degrees. The legacy 180 degree
  `shoulder_rotate` clearance deadband remains excluded from validation.
- `scripts/left_arm_v2_8_move_library.py` inherits the v2.6 shared adaptive parameters, then adds local corrections
  matched by seven-joint target-pose distance. Large pose changes create a new anchor.
- Before the final wrist motion, all six other joints must pass the tolerance. Failed checks save residuals and stop
  without an extra correction jerk.
- Wrist-induced hold errors learn once per completed wrist motion with bounded integral updates. The final seven-joint
  snapshot determines `training` or `validated` for each move.
- The nominal tolerance is 0.5 degrees. A 0.011 degree half-encoder-count comparison margin prevents an identical
  0.5027088 degree feedback bin from blocking forever; the effective acceptance edge is 0.511 degrees.

## bishop01 reference and validation

`bishop01` was overwritten on 2026-09-10 19:20:31 with this saved pose:

| Joint | Target (rad) |
|---|---:|
| shoulder_front | 0.744449556 |
| shoulder_side | -2.776188374 |
| shoulder_rotate | 2.702944994 |
| elbow | 0.392729074 |
| arm_roll | -2.773899555 |
| wrist_side | 0.167658508 |
| wrist | -2.609864950 |

The session used one Capture and seven Home → Replay cycles. Total Replay runtime was 452.832 seconds. Runs 1–4
trained the pre-wrist pose; runs 5–7 trained wrist-induced holding. Run 7 returned code 0 and recorded `validated`.

Final signed errors:

| Joint | Error (deg) |
|---|---:|
| shoulder_front | -0.174853 |
| shoulder_side | -0.218580 |
| shoulder_rotate | 0.480859 |
| elbow | 0.152998 |
| arm_roll | -0.131153 |
| wrist_side | 0.109285 |
| wrist | 0.240423 |

The maximum absolute error was 0.480859 degrees. Full per-run values are in
`scripts/data/bishop01_replay_training_20260910.json`; the table and charts are in
`outputs/bishop01_training_20260910/bishop01_training_report_20260910.xlsx`.

## Data that must remain on the Orin

Do not delete, regenerate or overwrite these while deploying code:

```text
/home/nvidia/hive_robot/DM_Control_Python/data/left_arm_v2_8_local_target_bias.json
/home/nvidia/hive_robot/DM_Control_Python/data/left_arm_v2_8_clearance_bias.json
/home/nvidia/hive_robot/DM_Control_Python/data/left_arm_v2_table_clearance.json
/home/nvidia/hive_robot/DM_Control_Python/data/left_arm_v2_home.json
/home/nvidia/hive_robot/DM_Control_Python/data/left_arm_v2_6_saved_moves.json
/home/nvidia/hive_robot/datasets/chess_pieces_yolo/
/home/nvidia/hive_robot/runs/chess_piece_yolo/
```

Also preserve every v2.6 adaptive-tuning JSON referenced by `left_arm_v2_6.py`; v2.8 uses those values as its shared
baseline. The authoritative v2.8 bias files could not be copied into this commit because SSH access from the Windows
machine was rejected. The supplied logs were therefore converted into the committed bishop01 session JSON. At the
start of the next session, back up the two v2.8 bias files before changing any replay-learning code.

## Verification completed

- Python syntax compilation for all changed/new Python files.
- Unit tests for height sampling/model behavior, YOLO fusion, v2.8 integration, crown geometry, Home and local replay
  bias.
- Spreadsheet formulas, chart bindings and rendered layouts were checked; no formula errors were found.
- Real Orin validation: clearance reported `validated`; bishop01 Replay run 7 reported `validated` with return code 0.

## 2026-09-11 Replay 后夹爪选项

- v2.8 的机械臂页面在 Replay 按钮旁增加了 `Replay 后合拢夹爪` 复选框，默认不勾选。
- 勾选后，浏览器等待所选保存动作的 Replay 进程结束，再调用原有 `/api/action/claw-close`；夹爪继续使用
  v2.6 已有的压力/堵转检测、回退和保持逻辑。
- 此选项只按流程先后顺序触发，不增加新的关节到位验证，也不根据 Replay 返回码筛选。
- Home、clearance 和独立的 Claw Close 均未改变；claw 没有加入 Home 关节集合。

## Next work

1. Back up the authoritative Orin v2.8 local and clearance bias JSON files.
2. Run one later cold-start confirmation of `Home → bishop01` to measure repeatability without continuing training
   unless a joint exceeds the gate.
3. Complete joint-axis vectors, motor zero offsets, direction signs, limits and TCP offset calibration.
4. Add enough independent board-to-arm reference points to bring held-out error below the chosen motion tolerance.
5. Implement IK and collision checks only after the transform and kinematic chain are validated.
6. Connect recognized D4 pieces to a dry-run grasp plan, then validate crown approach and retreat before enabling
   automatic execution.
