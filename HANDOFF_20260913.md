# HiveRobot handoff — 2026-09-13

## 今日结果

1. v2.8 机械臂页把 Placement1、Placement_C4、White Bishop Placement、White Knight Place_B1 集中到“多流程”下拉框，保留 `none` 和原 Saved Move Replay。`white_knight_place_b1` 接在 C4 夹取、Placement_C4 回 Clearance 之后，末尾执行 Claw Home；最终张爪不受放置姿态训练门禁阻挡。现有 Home 逻辑不变。
2. `white_knight_place_b1` 的 saved move 姿态登记为几何库 `placement_anchors.white_knight:b1`。名义棋冠 TCP 为 `[82.5, 27.5, 55] mm`；操作者确认棋子成功放到 b1。锚点仍为 `validated=false`，因为当时七关节中 `arm_roll` 超过 0.5°。机器人端已通过几何页的预览/合并写入此锚点，回读 revision `d7e1fc3887576fab04f4`，写入前的备份为 `data/chess_crown_geometry_v2_8.json.bak.1789337114992058732`。本机权威副本是 `scripts/data/chess_crown_geometry_v2_8.json`。
3. 排查反复回放时 B1 `arm_roll` 补偿总从同一旧样本开始的问题：多流程源姿态 C4 的局部学习对象先加载，B1 对象后学习；结尾源对象写回整份共享 JSON，覆盖 B1 的新记录。`LocalTargetBias._save()` 已改为从磁盘重读后只替换本对象所属锚点。这个修复也覆盖 bishop01 → white_bishop_place 的同类多流程路径。新构建标记 `v2.8-multiflow-anchor-save-merge` 已出现在机器人 20:44 与 20:50 的日志中，B1 `arm_roll` 保持补偿样本连续增长到 3，证明本轮未再被覆盖。旧版已经覆盖的数据无法从现有学习文件自动恢复。
4. B1 `arm_roll` 偏差发生在最后转 wrist 时，故把该阶段造成的 `arm_roll` 残差写入姿态局部的 wrist 保持补偿，不再当成预 wrist 的姿态目标偏差。已对一条已知错误 B1 目标偏差加入有条件的一次性回滚；保留其他学习参数。对 Clearance `wrist_side` 的局部保持学习加入“比历史最佳还差时回退”的判断，防止连续小幅变坏还继续积分。
5. Web 输出以往只保留最后约 8 KB，导致“去 C4 抖动”的前半段被截掉。v2.8 Web 现在为每次机械臂命令保存完整文本日志，页面新增“完整运行日志”链接 `/api/arm/full-run-log`，同时保留原先精简的实时输出。今天贴出的 15:39 和 15:44 两轮只能证明用户目测两轮仍抖动；当时没有完整 C4 电机轨迹，不能据此断言是哪一个电机力矩不足。后续 20:24 用户明确报告一轮无抖动。
6. 用户讨论了给 white bishop 棋冠涂色：现有标注图不需删除；新外观仍按 `white_bishop` 标注、分配到 train/val/test 后重新训练。旧外观若不再使用，可在新数据集副本里做去除旧图的对比实验，不覆盖原训练数据。

## 实机数据与判读

今天用户提供的 16 份原始粘贴日志（14 个不同开始时间）逐份归档于 `outputs/robot_runs_20260913/`，`index.json` 记录来源附件 ID、哈希、时间、返回码及可解析的训练状态。原始文件可能只有 Web stdout 尾段；不能把它们标成完整电机日志。

当晚一次只读 `/api/moves` 响应还显示四条今天新建的 saved moves：`white_pawn_D5`、`white_pawn_place_D2`、`white_rook_A5`、`white_rook_place_A1`。其七关节姿态和创建时间已按当时返回值记录在 `outputs/robot_runs_20260913/robot_saved_moves_observed.json`；这是部分观察记录，**不是**完整 saved-moves 文件的备份，也不代表这些姿态已训练或验证。

| 运行开始 | 返回码 | B1 最终主要误差 | Placement_C4 → Clearance 主要误差 | 结论 |
|---|---:|---|---|---|
| 14:20:48 | 1 | wrist −0.656° | 详见原日志 | 当时末端训练门禁仍导致失败；后来按用户要求取消对最终张爪的门禁。 |
| 14:29:51 / 14:34:48 | 1 | 转 wrist 前 `arm_roll` −0.765° | 详见原日志 | 0.5° 门禁在最终 wrist 前停止，未做生硬补正。 |
| 14:55:19 | 0 | `arm_roll` +0.546° | `wrist_side` +3.191° | 用户确认 B1 成功放置，姿态训练尚未全数通过。 |
| 15:05:14 / 15:16:39 | 1 | 转 wrist 前 `arm_roll` −0.546° / −0.525° | 详见原日志 | wrist 前门禁停止；后续训练和保持策略继续调整。 |
| 15:26:05 | 0 | `arm_roll` +0.874°、`wrist_side` +0.546° | `wrist_side` +3.410° | 多流程完成，但不是训练完成。 |
| 15:39:47 / 15:44:54 | 0 | `arm_roll` +0.874° / +0.721° | `wrist_side` +2.557° / +2.579° | 用户报告去 C4 时仍有抖动；当时日志前段被 8 KB 限制截掉。 |
| 20:24:03 / 20:37:07 | 0 | `arm_roll` +0.918° / +0.896° | `wrist_side` +2.579° / +2.623° | 学习样本仍重复从旧值开始，确认共享文件覆盖问题。20:24 用户称该轮无抖动。 |
| 20:44:20 | 0 | `arm_roll` +0.525°、wrist −0.831° | `wrist_side` +2.973° | 新保存逻辑已部署，B1 `arm_roll` 保持补偿保存为 +0.826°、样本数 3。 |
| 20:50:32 | 0 | `arm_roll` +0.066°、wrist −0.831° | `wrist_side` +3.453° | B1 除 wrist 外七关节目标中的其他六个均在 0.5° 内；B1 仍报 `training incomplete`。 |

最后一次 B1 wrist 在 Claw Home **之前**已误差 −0.831°；张爪过程中又偏移约 −1.880°。这与操作者观察到的棋子触碰棋盘、顶动 wrist 相符，但单凭电机日志不能证明接触原因。目前程序仍会把 B1 放置末端的 wrist 偏差用于目标学习（20:50 更新为 −0.083°）；在确认机械接触后，不应持续用该偏差把爪子压向棋盘。最后一轮 B1 的 `arm_roll` 从转 wrist 前 −0.372° 变为转完后 +0.066°，表明新保持补偿有效。

Placement_C4 的 Clearance `wrist_side` 是独立未解决项，20:50 残差 +3.453°，实际反馈位置 0.24548 rad、命令目标 0.33259 rad、反馈力矩约 +1.375（日志单位未标注，不能据此断定额定力矩不足）。按用户要求，此阶段记录 `training incomplete` 但不阻断后续 B1 流程。`returncode=0` 仅表示多流程执行完毕。

## 代码与数据

- `scripts/chessboard_vision_v2_8_web_control.py`：多流程下拉框和完整机械臂运行日志入口；Web 构建标记 `v2.8-full-arm-run-log`。
- `scripts/left_arm_v2_8_move_library.py`：B1 wrist 诱发 `arm_roll` 的局部保持学习、历史误学记录回滚、学习文件按锚点合并保存、末端学习持久化回显；动作构建标记 `v2.8-multiflow-anchor-save-merge`。
- `scripts/test_chess_v28_integration.py` 与 `scripts/test_left_arm_v28_local_bias.py`：覆盖 Web 全量日志、同文件多锚点写回和 B1 wrist 保持学习。
- `scripts/data/chess_crown_geometry_v2_8.json` 与 `scripts/CHESS_CROWN_GEOMETRY_V2_8_MEASUREMENTS.md`：B1 放置锚点及示教验证说明。现有 bishop/d4、bishop/c1、knight/c4 锚点保留。
- `outputs/robot_runs_20260913/`：16 份原始用户粘贴日志、`index.json` 与 README。
- `outputs/robot_runs_20260913/robot_saved_moves_observed.json`：四条当晚通过 Web API 观察到的新 saved moves 姿态；仅为部分记录。

原 v2.6 文件未改。未从机器人端导出私有学习 JSON、完整 saved moves 文件或相机/YOLO 数据集。收尾时机器人 `192.168.0.199` 的 SSH 连接超时，Web `/api/moves`、`/api/crown/status`、`/api/state` 也超时；因此 GitHub 同步的“数据”范围是本机几何 JSON、用户提供的运行输出和本 handoff，不代表机器人所有运行时训练文件已有备份。不要以仓库副本覆盖机器人 `DM_Control_Python/data/`。

## 下次工作入口

1. 机器人联机后，先单独备份其 `DM_Control_Python/data/`，尤其是 `left_arm_v2_8_local_target_bias.json`、Placement_C4/Placement1 Clearance 偏差文件、saved moves 和几何 JSON。保留现有机器人版本与时间戳；不要在机器人端整库 `git pull`。
2. 若 C4 运动再次抖动，使用新版 Web“完整运行日志”保存整段 C4 初始 Replay，结合用户观察的抖动时刻定位到关节/控制阶段，再修改控制参数。先前 8 KB 尾日志不足以判断。
3. 单独分析 Placement_C4 → Clearance 的 `wrist_side` +3.453° 残差与位置/力矩反馈，保持“未到位不阻断后续动作”的用户要求，同时避免继续让不改善的偏差学习累积。
4. 核实 B1 wrist 是否由棋子触板造成。若确实接触，应把机械接触与自由空间到位误差分开记录，避免把触板误差继续学习成 wrist 目标补偿；目前 B1 只有 wrist 未通过 0.5°。
5. 几何库目前是示教锚点，不是已校验的棋盘坐标→机械臂自动抓取模型；继续外推其他格前仍须核验标定、工具中心和避碰。

## 验证与 GitHub

本地 51 项 v2.8 控制/Web/几何测试通过，相关 Python 文件编译检查通过。代码改动的 `git diff --check` 通过；原始机器人日志有输出自带的尾随空格，为保持与来源附件逐字节一致而保留。提交、推送与远端校验结果以本次收尾命令为准。
