# HiveRobot handoff — 2026-09-11

## 本日目标与完成状态

今天完成了 `bishop01` 抓取后的 Placement1 流程：机械臂保持夹爪压力和各关节支撑，先将 wrist 单独回撤到 Table Clearance 高度，再让其余六个关节联动到 Clearance。流程已经可以稳定执行、不再因末端抖动或残差停止下一步，并会在最后明确报告训练是否进入 `0.5°`。

当前完整流程为：

1. 前置 Table Clearance 检查及受限残差修正。
2. Replay `bishop01`，在 final wrist 前验证其他关节。
3. final wrist 到棋冠夹取位置。
4. 使用现有压力停止逻辑合拢 claw，同时继承 final-wrist 控制器的目标、增益和前馈力矩。
5. wrist 单独回撤到 Clearance，仍保持其他关节和 claw。
6. 其余六个关节联动到 Clearance，wrist 和 claw 保持。
7. 平滑保持轨迹末端控制器 1.5 秒后测量、学习并打印最终训练状态。

Placement2（从 Clearance 将 bishop 放到开局目标格并释放）尚未实现。

## 今日关键代码变更

- `1a7b7bf`：Replay 后增加可选的原有 Claw Close 流程，Home 不包含 claw。
- `4023ada`：加入 Placement1，连接 bishop01、夹取和 Clearance。
- `29cafe2`、`ae8621e`：修正夹爪闭合时关节下坠；无缝继承 final-wrist 控制状态。
- `e497a89`：允许 Claw Home 中止 Placement1 后接管 claw。
- `af2b158`：按用户要求撤销错误的独立 claw 稳定修改，恢复原 claw 行为。
- `21cabd7`、`77c458c`、`293c716`：隔离 Placement1 学习数据，恢复并备份被早期 Placement1 污染的共享 Clearance 历史。
- `0dbc7c3`、`966b6d8`、`245565e`、`ddb44ea`、`c3eef12`、`162d5fa`：为普通 Clearance 加入事务式验证和最多三次 wrist_side 残差修正；修正过程中保持各关节的实时负载。
- `b1da6f9`、`ec3738f`、`9273e4b`、`f689017`：修复 bishop01 局部学习回退，恢复被旧算法破坏的记录，并为 wrist_side 加入负载前馈。
- `395ab79`：Placement1 改为 wrist 先行回撤，避免手臂横扫棋盘上的其他棋子；联动阶段包含 shoulder_rotate。
- `ff4948b`：联动结束后保留轨迹末端增益和力矩 1.5 秒，消除接近终点时的抖动。
- `c711baa`、`82899cd`：学习误差恶化时回退到较好偏置，并记录被拒绝的探索步，避免反复尝试同一坏值。
- `46aa87d`、`b91c4dd`：Clearance 和 Placement1 的 `0.5°` 残差变为非阻塞；仍测量和学习，但允许后续动作继续。wrist 先行回撤超过 `5°` 仍会安全停止。
- `6a3bf90`：仅将 Placement1 局部补偿范围扩展到 `±5°`；共享 Clearance 和其他动作仍为 `±3°`。
- `6aad0d5`：完整流程最后一行固定打印 `Placement1 final training status`、七关节误差和超差关节。

## 学习数据隔离

继续保持以下数据职责，不能再次混用：

- `data/left_arm_v2_8_clearance_bias.json`：普通 Table Clearance 的共享学习基线。
- `data/left_arm_v2_8_placement1_clearance_bias.json`：携带棋子时 Placement1 独立学习。
- `data/left_arm_v2_8_local_target_bias.json`：保存动作和 pose-local 学习。
- `data/left_arm_v2_table_clearance.json`：人工 capture 的名义 Clearance 姿态。
- `data/left_arm_v2_6_saved_moves.json`：包括最新人工 capture 的 `bishop01`。

共享 Clearance 的历史恢复锚点为 `8b20a284d5592a0c`。恢复值包括：

| 关节 | 恢复的共享偏置 |
|---|---:|
| arm_roll | 0.3671909698° |
| wrist_side | 0.6578945784° |
| wrist | -0.8207468246° |

恢复操作不会打开电机或产生运动，并会建立一次外部备份 `left_arm_v2_8_clearance_bias.json.pre_placement1_restored.json`。

## 最后一轮实机验证

运行时间：2026-09-11 21:53:48 至 21:56:11，耗时 143.212 秒，`returncode=0`。原始 stdout 保存在 `outputs/placement1_training_20260911/placement1_20260911_215348.log`。

关键结果：

- claw 压力接触成功：`contact=true`。
- wrist 先行回撤经过两次修正，最终误差 `-0.0656°`。
- 平滑末端保持日志出现，未报告末端抖动。
- 非阻塞规则生效，超差后仍以成功状态结束并保存学习。
- 最后一行正确打印 `status=training incomplete`。

Placement1 对名义 Clearance 的最终误差：

| 关节 | 最终误差 | 0.5°训练目标 |
|---|---:|---|
| shoulder_front | +0.3279° | 达标 |
| shoulder_side | +0.3060° | 达标 |
| elbow | +0.4590° | 达标 |
| shoulder_rotate | -1.7923° | 未达标 |
| arm_roll | +0.0219° | 达标 |
| wrist_side | +2.6665° | 未达标 |
| wrist | +0.1749° | 达标 |

编码器半计数余量为 `0.011°`，内部有效比较门限为 `0.511°`；用户界面和最终状态仍显示训练目标 `0.5°`。

本轮使用的 Placement1 局部偏置为：

| 关节 | 本轮局部偏置 |
|---|---:|
| shoulder_front | +0.5814° |
| shoulder_side | +0.8109° |
| elbow | +1.8142° |
| shoulder_rotate | -3.0000° |
| arm_roll | -0.4000° |
| wrist_side | +2.2000° |
| wrist | +0.4000° |

为下一轮保存的更新：

- shoulder_rotate：`-3.0° → -3.4°`，样本数 9。
- wrist_side：`+2.2° → +2.4°`，步长比例 0.5，样本数 12。
- wrist_side 还叠加只读共享 Clearance 偏置 `+1.0098°`，所以下一轮总命令偏置约为 `+3.4098°`。

注意日志里的 `v2.8 final verification={status: validated}` 是前置/保存动作验证结果，不能代替 Placement1 最终训练状态。判断 Placement1 是否完成必须读取 stdout 最后一行 `v2.8 Placement1 final training status=`。本轮最终状态明确为 `training incomplete`。

## 下次工作入口

1. 先备份 Orin 上四个权威运行数据文件，尤其是 `left_arm_v2_8_placement1_clearance_bias.json`，不要用仓库副本覆盖实机学习记录。
2. 在机械布局不变的情况下再执行一次完整 `bishop01 + Placement1`；确认本轮实际读取到 shoulder_rotate `-3.4°`、wrist_side 局部 `+2.4°`。
3. 检查最终最后一行。若仍为 `training incomplete`，以其中的 `blocking_errors_deg` 继续学习；不要因前面的 `final verification=validated` 停止训练。
4. 继续观察 wrist 回撤后再联动、claw 持压以及联动末端是否平稳。任何明显抖动、下坠或扫过棋子均优先于角度学习处理。
5. 两个剩余关节稳定进入 `0.5°` 后，冻结或备份这组 Placement1 学习数据，再开始 Placement2：Clearance 到 bishop 开局目标格、垂直下降、松爪和安全撤离。

## 同步状态

截至本 handoff 创建前，今天的功能代码提交均已在 `main`，并与 `origin/main` 对齐。本 handoff 和最后一轮原始实机日志将在同一后续提交中同步。

记录提交前在 Windows 工作区重新运行了 `test_left_arm_v28_local_bias` 和 `test_left_arm_v28_home`，共 36 项测试通过；`left_arm_v2_8.py`、`left_arm_v2_8_move_library.py` 和 `chessboard_vision_v2_8_web_control.py` 也通过 Python 语法编译检查。
