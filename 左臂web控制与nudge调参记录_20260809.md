# 左臂 web 控制与 nudge 调参记录 2026-08-09

## 范围

本次记录基于 `left_arm_v2_5_3.py` 和新的 `left_arm_v2_5_3_web_control.py`。

目标：

- 新建左臂 web 控制平台。
- 在网页中显示 RGB-D 视频、绿色 ROI 框和 XYZ depth 数据。
- 支持 Capture New Home、Capture New Clearance、Table Clearance Move、Home Move、Emergency Stop。
- 增加 Debug Command / Debug Output。
- Debug Output 增加 Copy 按钮。
- 增加各关节独立 nudge 控制，web controller 版本记为 v1.2。
- 修复 clearance holding position 后单独控制 wrist 时的抖动和 shoulder_front 下坠。

## 新物理模型

本轮确认 `shoulder` 坐标系统一以 `shoulder_front` 轴心为参考点。

当前 web controller 默认相机参数：

```python
DEFAULT_CAMERA_FORWARD_FROM_SHOULDER_CM = 10.5
DEFAULT_CAMERA_LEFT_FROM_SHOULDER_CM = -13.0
DEFAULT_CAMERA_UP_FROM_SHOULDER_CM = 12.0
DEFAULT_CAMERA_PITCH_DOWN_DEG = 70.0
```

方向约定：

```text
forward: 从 shoulder_front 向机器人前方为正
left:    从机器人视角向左为正，向右为负
up:      相机高于 shoulder_front 为正
pitch:   相机光轴相对水平向下俯视角
```

## web controller v1.2

新增文件：

```text
scripts/left_arm_v2_5_3_web_control.py
scripts/LEFT_ARM_V2_5_3_WEB_CONTROL.md
```

启动流程：

```bash
cd ~/ascam_ws
source ~/.bashrc
roslaunch ascamera hp60c.launch
```

另开窗口：

```bash
cd ~/hive_robot/DM_Control_Python
python3 left_arm_v2_5_3_web_control.py --enable-execute
```

若没有 sudoers，web 执行动作会报：

```text
sudo: a password is required
```

需要先配置 sudoers 或用 `--no-sudo`。

## 视频流与 XYZ

web controller 已补回旧版 HP60C 相关显示：

- RGB + Depth MJPEG 流。
- 单独 RGB 流。
- 单独 Depth 流。
- 绿色 ROI 框，默认：

```text
x0=55, y0=115, x1=605, y1=455
```

- ROI 内目标检测。
- 视频左上角叠加：

```text
F = forward cm
L = left cm
U = up cm
```

- 右侧 Status 面板显示：

```text
Forward
Left
Up
Depth
```

- `/api/state` 返回：

```text
target
target_error
target_age_s
```

## home 调整

home 已按 clearance 的方式改为主联动：

```text
elbow
shoulder_front
shoulder_side
arm_roll
wrist_side
wrist
```

`shoulder_rotate` 后续单独处理。

home 主联动参数：

```python
COUPLED_HOME_MAX_SECONDS = 16.0
COUPLED_HOME_CONTROL_DT = 0.01
COUPLED_HOME_TRAJECTORY = "blend_smootherstep"
COUPLED_HOME_LINEAR_BLEND = 0.35
COUPLED_HOME_COMMAND_BIAS_DEG = {"shoulder_side": 1.0}
```

home wrist 延迟窗口：

```python
COUPLED_HOME_PROGRESS_WINDOWS = {
    "arm_roll": (0.65, 1.0),
    "wrist": (0.35, 1.0),
}
COUPLED_HOME_PRE_WINDOW_GAINS = {
    "wrist": {"kp": 6.0, "kd": 0.8},
}
```

home `shoulder_side` 结果稳定在约 0.3 deg 误差内。

## clearance 新旧姿态对比

新 capture clearance：

```json
{
  "shoulder_front": 1.9823377132415771,
  "shoulder_side": -2.738422155380249,
  "shoulder_rotate": 2.395857095718384,
  "elbow": 0.3240634799003601,
  "arm_roll": -2.9673075675964355,
  "wrist_side": 0.33665215969085693,
  "wrist": -1.0004196166992188
}
```

相对旧 clearance 的变化：

```text
shoulder_front   -0.76 deg
shoulder_side    -1.07 deg
shoulder_rotate  +2.12 deg
elbow            -2.86 deg
arm_roll         +0.20 deg
wrist_side       -0.61 deg
wrist            +7.50 deg
```

主要变化是 wrist 明显收回，elbow 少向前，shoulder_rotate 多向外旋一点。

## web v1.2 独立关节控制

web controller 增加 Joint Nudge 面板：

```text
2 deg
5 deg
10 deg
```

关节方向按物理世界命名：

```text
shoulder_front:   Backward / Forward
shoulder_side:    Outward / Inward
elbow:            Forward / Backward
shoulder_rotate:  Outward Rotate / Inward Rotate
arm_roll:         Inward Roll / Outward Roll
wrist_side:       Wrist Outward / Wrist Inward
wrist:            Wrist Forward / Wrist Backward
```

正负度数与物理方向对应关系：

```text
shoulder_front
+deg = 手臂向后
-deg = 手臂向前

shoulder_side
+deg = 手臂向外
-deg = 手臂向内

elbow
+deg = 手臂向前
-deg = 手臂向后

shoulder_rotate
+deg = 肩部向外旋转
-deg = 肩部向内旋转

arm_roll
+deg = 手臂向内旋转
-deg = 手臂向外旋转

wrist_side
+deg = wrist 向外
-deg = wrist 向内

wrist
+deg = wrist 向前
-deg = wrist 向后
```

web controller v1.2 中按钮到度数的映射：

```text
shoulder_front Backward       -> +deg
shoulder_front Forward        -> -deg
shoulder_side Outward         -> +deg
shoulder_side Inward          -> -deg
elbow Forward                 -> +deg
elbow Backward                -> -deg
shoulder_rotate Outward Rotate -> +deg
shoulder_rotate Inward Rotate  -> -deg
arm_roll Inward Roll          -> +deg
arm_roll Outward Roll         -> -deg
wrist_side Wrist Outward      -> +deg
wrist_side Wrist Inward       -> -deg
wrist Wrist Forward           -> +deg
wrist Wrist Backward          -> -deg
```

## nudge-hold 修复

原 web nudge 只控制单个关节，没有 hold 其它 6 个关节。clearance holding position 后单独动 wrist 时，会出现 wrist 先下掉或抖动。

新增：

```bash
left_arm_v2_5_3.py nudge-hold
```

特点：

- 主动关节按指定角度运动。
- 其它 6 个关节保持当前姿态。
- 只允许单关节。
- web 只允许 2/5/10 deg。
- web 同一时刻只允许一个动作运行。

web wrist forward 5 deg 示例：

```bash
sudo -n python3 left_arm_v2_5_3.py nudge-hold \
  --joint wrist \
  --deg 5.000 \
  --seconds 6.00 \
  --max-deg 10 \
  --hold-joints shoulder_front,shoulder_side,shoulder_rotate,elbow,arm_roll,wrist_side,wrist \
  --auto-gains
```

## wrist 单独控制稳定参数

初版 `nudge-hold` 虽然 hold 了其它关节，但 wrist 仍使用普通 nudge 参数：

```text
kp=18.0, kd=1.2, seconds=2.2
```

该参数在 clearance holding position 后仍可能抖。

已改为复用 clearance wrist fine 的稳定参数：

```python
COUPLED_CLEARANCE_WRIST_FINE_GAINS = {
    "kp": 26.0,
    "kd": 4.7,
    "seconds": 6.0,
}
```

同时 wrist nudge 时，`arm_roll` 使用 clearance wrist fine 中验证过的软保持：

```python
CLEARANCE_COMPLIANT_HOLD_GAINS["arm_roll"] = {"kp": 6.0, "kd": 6.0}
```

## shoulder_front 下坠修复

wrist nudge 稳定后，发现 `shoulder_front` 在 nudge 期间下坠约 1.88 deg。

原因：`nudge-hold` 漏传 clearance 阶段用于托住 shoulder_front 的 hold torque。

已修复：

```python
hold_tau=CLEARANCE_JOINT_HOLD_TAU
```

其中：

```python
CLEARANCE_JOINT_HOLD_TAU = {
    "shoulder_front": 2.5,
}
```

复测后：

- wrist 不抖。
- shoulder_front 不再下坠。

## 独立关节风险排序

当前 nudge-hold 机制对所有关节都会 hold 其它 6 个关节，但不同关节风险不同。

建议风险排序：

```text
低风险:
wrist
wrist_side

中低风险:
arm_roll
shoulder_rotate

中风险:
shoulder_side

中高风险:
shoulder_front

高风险:
elbow
```

建议测试顺序：

```text
1. wrist 5 deg
2. wrist_side 2 deg / 5 deg
3. arm_roll 2 deg
4. shoulder_rotate 2 deg
5. shoulder_side 2 deg
6. shoulder_front 2 deg
```

暂不建议在 clearance 后用 web nudge 动 elbow。

## 验证

在 Windows 本地完成源码语法解析：

```bash
compile(left_arm_v2_5_3.py)
compile(left_arm_v2_5_3_web_control.py)
```

web controller 命令构造检查通过。

实际 Orin 侧测试：

- clearance 10s 主联动稳定。
- home 16s 六联动稳定。
- wrist forward 5 deg nudge-hold：无抖动。
- 修复 hold_tau 后 wrist nudge 期间 shoulder_front 不下坠。
