# 桌面抓取闭环测试记录 2026-07-09

## 测试目标

建立左臂从视觉目标坐标到桌面抓取动作的第一版闭环流程，并验证以下关键能力：

- 读取每个达妙电机当前位置。
- 建立关节名称、电机 ID、Home 姿态和安全姿态。
- 根据粗机械臂坐标 `arm_x/arm_y/arm_z` 生成保守抓取姿态。
- 按安全顺序执行肩前摆、手肘、肩侧摆、手腕和爪夹动作。
- 爪夹持续合拢，直到检测到接触压力。
- 抓取后尝试垂直抬起目标物。

本轮测试重点是建立可重复、可记录、可逐步调参的桌面抓取实验链路，并完成从视觉目标、机械臂接近、爪夹压力闭合到目标物提起的完整闭环。

## 硬件状态

控制平台：

```text
Jetson Orin
USB-CAN
Damiao DM_Control_Python
串口：/dev/ttyACM0
波特率：921600
```

左臂电机配置：

| 关节名称 | 电机型号 | Slave ID | Master ID |
|---|---|---:|---:|
| shoulder_front 左肩前摆 | DM4340 | 0x1E | 0x0E |
| shoulder_side 左肩侧摆 | DM4340 | 0x1F | 0x0F |
| shoulder_rotate 左肩旋转 | DM4340 | 0x20 | 0x10 |
| elbow 左肘 | DM4340 | 0x21 | 0x11 |
| arm_roll 左臂旋转 | DM4340 | 0x22 | 0x12 |
| wrist 左手腕 | DM4310 | 0x29 | 0x19 |
| claw 左爪 | DM4310 | 0x28 | 0x18 |

## Home 姿态

本轮正式控制脚本使用 `left_arm_home.json` 保存 Home。当前 Home 由实物姿态采集得到：

```json
{
  "shoulder_front": 0.8360036611557007,
  "shoulder_side": -2.780003070831299,
  "shoulder_rotate": -1.7408636808395386,
  "elbow": -1.0664149522781372,
  "arm_roll": 2.0243000984191895,
  "wrist": 1.1339360475540161
}
```

爪子最大安全打开位置沿用前序测试：

```text
Q_OPEN ≈ -3.88056 rad
```

## 视觉输入与粗机械臂坐标

摄像头侧得到的目标中心坐标：

```text
camera_x = -1.9 cm
camera_y = +0.7 cm
camera_z = 68.0 cm
```

初始粗机械臂坐标：

```text
arm_x = +16.5 cm
arm_y = +40.6 cm
arm_z = +40.6 cm
```

后续为了实际靠近目标物，调参测试使用：

```text
arm_x = 16.5 cm
arm_y = 21.6 cm
arm_z = 40.6 cm
```

粗 IK 当前只使用：

- `arm_z` 和水平距离控制 `shoulder_front`。
- `arm_y` 控制 `shoulder_side`。
- `arm_x` 暂未进入执行控制。

## 当前抓取姿态参数

最终较接近目标物的一组参数：

```text
shoulder_front offset = -50 deg
shoulder_side offset = -19 deg
elbow offset = +42 deg
wrist down offset = -54 deg
```

对应执行命令：

```bash
sudo python3 left_arm_controller.py target --x 16.5 --y 21.6 --z 40.6 --execute
```

程序规划值：

```json
{
  "shoulder_front": -0.03666096484146408,
  "shoulder_side": -3.1116156287102212,
  "shoulder_rotate": -1.7408636808395386,
  "elbow": -0.3333766664405188,
  "arm_roll": 2.0243000984191895,
  "wrist": 1.1339360475540161
}
```

实际执行时，手腕会在第 6 步下压到：

```text
wrist ≈ 1.1339 rad - 54 deg ≈ 0.191 rad
```

## 安全动作顺序

当前安全动作链路如下：

1. `shoulder_front` 先向后到安全角，避免手腕扫桌。
2. `elbow` 向前折叠到安全角，其他关节保持。
3. `shoulder_front` 到目标抓取角，手肘保持安全。
4. `elbow` 到目标抓取角，肩前摆保持。
5. `shoulder_side` 到目标侧摆角。
6. `wrist` 向下到抓取角。
7. `claw` 持续合拢，直到压力接触。
8. 尝试抬起目标物。

Home 返回顺序经过碰撞测试后调整为：

1. `shoulder_side` 先回 Home。
2. `elbow` 回到安全角。
3. `shoulder_front` 回到安全角。
4. `elbow` 回 Home。
5. `shoulder_front` 回 Home。

该返回顺序比“手肘先回 Home”更安全，因为侧摆未归位时手肘回收会增加扫桌风险。

## 侧摆安全范围

前序结论：

- 前摆接近 90 度时，肩侧摆向内约 15 度为硬上限。
- 当前抓取姿态前摆约 50 度，肩侧摆仍有更多空间。
- 当前姿态下实测侧摆 25 度接近极限，21 度最接近目标物，19 度作为当前抓取测试值。

当前代码允许：

```text
shoulder_side test limit = +/-25 deg
```

## 爪夹压力检测

本轮使用持续合拢策略：

```text
claw pressure close mode: keep closing until contact or Ctrl+C
```

触发条件：

```text
CLAW_CONTACT_TAU_THRESHOLD = 0.30
CLAW_STALL_TAU_THRESHOLD = 0.24
CLAW_STALL_VEL_THRESHOLD = 0.08
```

典型接触检测数据：

```text
claw contact detected
pos = 11.241130828857422
vel = 0.622710645198822
tau = 0.3687423765659332
backoff hold pos = 11.201130828857423
```

另一组接触数据：

```text
pos = 11.2933931350708
vel = 0.593406617641449
tau = 0.38339439034461975
```

判断：

- 爪夹压力检测已能触发。
- 接触阈值当前偏保守，能避免无限合拢。
- 但仅凭电机 torque 判断抓取成功还不充分，需要加入抓取后抬升位移和物体视觉确认。

## 抬升测试

第一次抬升策略：

```text
shoulder_front +5 deg
elbow +5 deg
```

结果：

```text
用户目测：抬起不到 1 cm
```

第二次抬升策略：

```text
shoulder_front +15 deg
elbow +12 deg
```

结果：

```text
肩前摆明显向后收，末端没有形成理想垂直抬升。
```

第三次抬升策略：

```text
shoulder_front +0 deg
elbow +22 deg
wrist +10 deg
```

实际最终数据：

```text
shoulder_front pos = -0.01583123579621315
shoulder_side pos  = -3.1054017543792725
elbow pos          = 0.03833829239010811
wrist pos          = 0.3538185656070709
claw pos           = 11.21137523651123
```

对比规划：

- `shoulder_front` 基本保持在抓取角，没有明显后拉。
- `elbow` 实际上抬约 21 度，接近规划的 22 度。
- `wrist` 实际回正约 9 度，接近规划的 10 度。

判断：

```text
第 8 步确实执行了，手腕也动了。
但单靠手肘和手腕，末端高度变化仍不够。
```

主要限制：

- `shoulder_front tau ≈ -7.62`，肩前摆仍承担高负载。
- `elbow tau ≈ 3.99`，手肘接近本轮测试中的高负载区。
- 当前几何姿态下，肘关节抬升会带来末端路径变化，但不一定等价于末端垂直上移 5 cm。

## 详细测试流水

本节按当天实际调试顺序记录关键测试次数。由于每次完整日志较长，这里保留每轮的输入、参数变化、主要输出和人工观察结论，便于后续复现实验和制作 ISEF 海报。

| # | 测试内容 | 输入或参数 | 主要观察 | 结论 |
|---:|---|---|---|---|
| 1 | 读取全臂状态 | `left_arm_controller.py status` | 7 个电机均可 enable 并返回位置、速度、力矩 | 通信链路正常 |
| 2 | 采集 Home | `capture-home` | 生成 `left_arm_home.json` | Home 姿态建立完成 |
| 3 | Ready 姿态 | `ready` | 肩前摆力矩约 `-7.62`，肘力矩约 `3.03` | 可执行，但肩前摆负载较高 |
| 4 | Pre-grasp 姿态 | `pregrasp` | 肩前摆到约 `-0.708`，肘到约 `-0.477` | 姿态可达，但手腕有撞桌风险 |
| 5 | Home 返回 | `home` | 可回到采集 Home 附近 | 基础 Home 返回可用 |
| 6 | 目标干跑 | `target --x 16.5 --y 40.6 --z 40.6` | 只打印 planned targets，不执行 | 目标接口建立完成 |
| 7 | 初版执行 | `x=16.5 y=40.6 z=40.6 --execute` | 肩前摆接近 `-90°`，手腕会扫到桌面 | 路径不安全，需要先折叠避障 |
| 8 | 安全路径第 1 版 | 肩先后摆约 `8°`，再肘前折 | 手腕仍可能低，避障余量不足 | 肩后摆角度不够 |
| 9 | 肩后摆增大 | 肩后摆 `20°` | 触发安全限位问题 | 需要放宽或重新定义安全角 |
| 10 | 折叠安全角测试 | 肩后摆 `30°`，肘 `70°` | 比 20° 安全 | 继续增加手肘折叠 |
| 11 | 折叠安全角测试 | 肩后摆 `30°`，肘 `80°` | 更接近安全避障姿态 | 可继续测试 |
| 12 | 折叠安全角测试 | 肩后摆 `30°`，肘 `90°` | 安全性进一步提高 | 可继续测试 |
| 13 | 折叠安全角测试 | 肩后摆 `30°`，肘 `95°` | 安全 | 可继续测试 |
| 14 | 折叠安全角确认 | 肩后摆 `30°`，肘 `100°` | 用户确认安全 | 设为安全折叠姿态 |
| 15 | 安全 Home 路径 | 先折叠，再回 Home | Home 时仍需避免扫桌 | Home 返回需要分步 |
| 16 | Safe Home 第 1 版 | 肩回安全角，再肘回 Home | 仍有桌面扫到风险 | 顺序需调整 |
| 17 | Safe Home 第 2 版 | 先侧摆 Home，再肘到安全角，再肩回安全角 | 用户确认更安全 | Home 顺序确定 |
| 18 | 粗 IK 初版 | `x=16.5 y=40.6 z=40.6` | 肩前摆仍按 `-90°`，离目标策略不合理 | 前摆不能固定为 -90° |
| 19 | 粗 IK 距离映射 | `x=16.5 y=30.6 z=40.6` | 水平距离约 `34.77 cm`，前摆先映射到 `-70°` | 比 -90° 更接近目标 |
| 20 | 前摆继续调整 | `x=16.5 y=30.6 z=40.6`，前摆 `-50°` | 比 -70° 更接近目标 | 前摆近距离采用 `-50°` |
| 21 | Home 碰撞复盘 | 前摆 `-50°` 后直接 Home | Home 时撞桌，原因是侧摆未先回位 | Safe Home 必须先回侧摆 |
| 22 | 侧摆 15° 测试 | 当前前摆角，侧摆约 `15°` | 安全，有余量 | 当前前摆下 15° 不是极限 |
| 23 | 侧摆 18° 测试 | 当前前摆角，侧摆约 `18°` | 安全，接近目标改善 | 可继续增加 |
| 24 | 侧摆 20° 测试 | 当前前摆角，侧摆约 `20°` | 仍有余量 | 可继续增加 |
| 25 | 侧摆 22° 测试 | 当前前摆角，侧摆约 `22°` | 仍可用 | 接近目标继续改善 |
| 26 | 侧摆 25° 测试 | `x=16.5 y=15.6 z=40.6`，侧摆约 `-25°` | 接近机械安全极限，但仍未碰撞 | 当前前摆下 25° 为极限附近 |
| 27 | 侧摆 21° 复核 | 目标侧摆约 `-21°` | 用户观察最接近目标物 | 21° 是接近目标的最佳点之一 |
| 28 | 当前执行侧摆 | `x=16.5 y=21.6 z=40.6`，侧摆约 `-19°` | 接近目标且安全余量更好 | 采用 19° 作为当前测试值 |
| 29 | 手腕下探 20° | wrist down `-20°` | 手腕下压不足 | 需要更大下探角 |
| 30 | 手腕下探 50° | wrist down `-50°` | 下压过多 | 需要回调 |
| 31 | 手腕下探 40° | wrist down `-40°` | 可用但仍需微调 | 继续细调 |
| 32 | 手腕与侧摆联调 | 侧摆 `-18°`，手腕 `-35°` | 接近姿态改善 | 继续微调 |
| 33 | 手腕与侧摆联调 | 侧摆 `-19°`，手腕 `-38°` | 更接近目标 | 继续下探 |
| 34 | 手腕下探确认 | 侧摆 `-19°`，手腕约 `-40°` | 目标接近较好 | 可进入爪夹闭合 |
| 35 | 手腕再下探 | wrist down `-50°` | 更接近桌面目标 | 接近抓取姿态 |
| 36 | 手腕最终下探 | wrist down `-52°`，后续代码设到约 `-54°` | 抓取姿态较合适 | 作为当前抓取下探角 |
| 37 | 爪夹压力闭合第 1 次 | 固定目标闭合到约 `8.4 rad` | 未检测 contact，用户观察未完全合拢 | 改为未接触前持续合拢 |
| 38 | 爪夹压力闭合第 2 次 | 持续闭合到约 `10.1 rad` | 仍未触发，接近闭合极限 | 需要继续合拢直到压力 |
| 39 | 爪夹压力闭合第 3 次 | 持续闭合 | `pos≈11.50, tau≈0.354` 触发 contact | 压力检测成功 |
| 40 | 爪夹压力闭合第 4 次 | 持续闭合 | `pos≈11.25, tau≈0.335` 触发 contact | 接触阈值有效 |
| 41 | 爪夹压力闭合第 5 次 | 持续闭合 | `pos≈11.29, tau≈0.383` 触发 contact | 抓取闭合稳定复现 |
| 42 | 抬升第 1 版 | 肩前摆 `+5°`，肘 `+5°` | 目测抬起不到 `1 cm` | 抬升不足 |
| 43 | 抬升第 2 版 | 肩前摆 `+15°`，肘 `+12°` | 肩前摆明显向后收，非垂直抬升 | 抬升路径不理想 |
| 44 | 抬升第 3 版 | 肩前摆 `+0°`，肘 `+22°`，腕 `+10°` | 手肘和手腕确实动作，目标物完成提起动作 | 当前链路可完成抓取并提起 |

## 关键最终测试数据

最终抓取与提起测试命令：

```bash
sudo python3 left_arm_controller.py target --x 16.5 --y 21.6 --z 40.6 --execute
```

爪夹接触检测：

```text
claw contact detected
pos = 11.241130828857422
vel = 0.622710645198822
tau = 0.3687423765659332
backoff hold pos = 11.201130828857423
```

最终提起后关节状态：

```text
shoulder_front pos = -0.01583123579621315
shoulder_side pos  = -3.1054017543792725
shoulder_rotate pos = -1.7439154386520386
elbow pos          = 0.03833829239010811
arm_roll pos       = 2.0235371589660645
wrist pos          = 0.3538185656070709
claw pos           = 11.21137523651123
```

动作变化判断：

- 爪夹从最大安全打开位置开始持续合拢，最终通过 torque 阈值检测到接触。
- 手肘从抓取姿态继续上抬约 `21°`。
- 手腕从下探抓取姿态回正约 `9°`。
- 肩前摆基本保持抓取角，避免了大幅后拉。
- 用户观察确认：当前流程能够抓取目标物并完成提起动作。

## 当前结论

1. 左臂已经具备从目标坐标进入保守抓取姿态的基础能力。
2. 安全动作顺序已经明显降低手腕撞桌和 Home 返回扫桌风险。
3. 侧摆在当前前摆角度下可使用到约 25 度，实际目标使用 19 度较稳定。
4. 爪夹压力闭合已能检测接触，典型触发 torque 约 `0.35-0.38`。
5. 本日最终链路已经能够完成“接近目标物、压力闭合抓取、提起目标物”的闭环动作。
6. 提起高度仍需用深度相机或外部标尺定量记录；下一阶段目标是把“能提起”升级为“稳定垂直提起 5 cm”。

## 下一步建议

优先改进抬升策略：

```text
shoulder_front +5 deg
elbow +18 deg
wrist +10 deg
```

理由：

- 肩前摆轻微参与抬升，但避免大幅后拉。
- 手肘仍提供主要抬升。
- 手腕回正帮助爪子远离桌面。

同时建议增加两个观测量：

1. 抬升前后用深度相机读取目标物中心 `z` 变化。
2. 抬升后再次检测爪夹压力，如果压力突然下降，说明物体可能滑脱。

## ISEF 海报可用实验点

本轮工作适合整理成海报中的“工程迭代过程”：

- 问题：长臂桌面抓取存在手腕碰撞、肩部高负载、爪夹抓取不确定。
- 方法：分阶段安全轨迹、粗 IK、压力阈值闭合、逐步实测调参。
- 数据：关节角度、torque、接触位置、接触 torque、抬升前后姿态。
- 结果：完成从视觉目标到压力闭合抓取，并实现目标物提起；垂直抬升高度仍需进一步量化和优化。
- 下一步：加入末端高度反馈，把“关节抬升”升级为“末端 z 轴闭环抬升”。
