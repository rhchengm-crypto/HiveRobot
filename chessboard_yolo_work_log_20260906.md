# 棋盘 YOLO 工作记录 — 2026-09-06

## 当天结论

- d4 训练后在 d5 识别不稳定，并不意味着所有棋子必须在 64 格逐一训练。
- 实际训练拼图确认：旧标签按格子缩小到 72% 生成，漏掉棋冠，需改成完整棋子框。
- 修框、补充 d5 样本并重新训练后，用户确认四类白棋在 d5 基本能识别。
- 高白王还会在后方邻格产生深度重复候选；YOLO 本身只检测到一枚白王。
- 最终本地版本：`v2.7-full-board-crown-projection-v2-grid-boxes`。

## 标注、预测与界面修改

1. 新增 `scripts/chess_piece_yolo_labels.py`，主页面入口 Review YOLO Labels，地址 `/yolo-labels`。
   在原始训练/验证图片上重画框、修改类别、增删框；保存备份旧标签，清除标签缓存。
   以原图和标签内容摘要记录复核状态，修改任一文件后需重新复核；拒绝过期保存。
2. 新采样生成向上扩展的参考框，仍需人工确认覆盖棋冠、身体和底座。
   Web 训练检查 train/val 是否已复核；已有正确标签可以重复用于训练。
3. YOLO 格子映射使用完整框高度 90% 处的水平中点作为近似底座点。
   原始框中心保留为 `bbox_center_px`。该近似不等同于现场验证的精确抓取坐标。
4. 根据实际 `detections.bbox_xyxy` 绘制绿色预测框、类别和置信度。
   原始输入不变，另存 `_pred.jpg`；JSON 返回 prediction_image_path / prediction_image_url。
5. 最终 Inspect Square、Detect Whole Board、YOLO Detect Table 的识别图同时显示
   黄色棋盘网格、绿色框、类别和置信度（前提是运行 YOLO）。
   图像是该次输入的静态结果，移动棋子后需再次检测；Show Input Frame 返回实时原图。
   无 YOLO 结果时回退到网格叠加流。不要把带网格/预测文字的结果图作为训练原图。

## 模型与现场结果

当前建议继续使用现场确认的模型：

```text
/home/nvidia/hive_robot/runs/chess_piece_yolo/yolo11n_rank4-12/weights/best.pt
```

- rank4-10：修复标注后训练正常完成；第 48 轮最好，第 68 轮提前停止。
  白马在 d5 仍可能被误判为白后，曾出现 60.7%、80.1% 的错误置信度。
- rank4-11：补充 d5 白马后训练成功。
- rank4-12：用户确认 d5 基本能识别。提供的输出：白王 78.0%、白后 86.5%、
  白马 90.5%、白车 92.7%。三份白车框/置信度完全相同，不能当作三次独立验证。
- d5 参与训练后能识别属于进展，但未训练位置的泛化仍需单独测试。

最近有明确数量的记录来自 **rank4-11**（不是 rank4-12 的实时统计）：

| 类别 | train 标注框 | val 标注框 |
| --- | ---: | ---: |
| white_rook | 14 | 10 |
| white_knight | 39 | 21 |
| white_queen | 16 | 9 |
| white_king | 42 | 28 |
| 合计 | 111 | 68 |

白兵、白象和黑棋六类在该记录中无样本。需要同时关注数量平衡和位置/朝向/底色覆盖，
不要用大量几乎相同的连续照片代替场景变化。

## 高白王重复占格

- d5/d6：主体 7/7 帧，假影 1/7 帧；深度凸起约 3.1 cm / 9.3 cm。
- e5/e6：主体 7/7 帧，假影 3/7 帧；深度凸起约 2.6 cm / 8.7 cm。
- 初版固定少于 3 票漏掉 e6；v2 改为候选严格少于半数帧、主体超过半数帧。
  同时要求候选位于相邻主体 YOLO 框上部、深度高度差有支持证据、主体置信度足够。
- 在本次 YOLO 后同步修正 piece_results、detected_squares、detected_count、identified_pieces。
  排除原因记录为 `yolo_crown_depth_projection`，保留所属主体格和原检测诊断数据。
- 去掉实时显示中“只因邻近历史 YOLO 身份就删除未知格”的规则。
  稳定深度候选、独立 YOLO 检出的相邻棋子保留。
- 两份原始 JSON 本地回放分别只保留 d5、e5；这不等于实物全棋盘已通过。
  持续多数帧出现的假影仍可能保留，需要后续证据，不能靠扩大删除范围误删真实邻子。

## 验证与部署状态

```powershell
python -m unittest discover -s scripts -p 'test_chess*.py' -v
```

结束前 14 项测试通过：标签保存/复核/备份、无效输入、底座映射、预测图与网格组合、
图片 HTTP 路由、全棋盘邻接方向、两份高棋子数据及票数边界。
标注编辑器已做浏览器拖框保存实测；最终页面脚本通过 JavaScript 语法检查。

本地工作区是 Windows 镜像。Orin SSH BatchMode 登录曾报 Permission denied，
本次助手未直接向 Orin 部署或训练。用户随后贴出的版本和训练日志确认了中间版本的现场使用。
最后的 grid-boxes 组合显示改动已在本地完成，尚未收到用户现场验证结果。
模型权重、原始训练集位于 Orin，本次 GitHub 同步范围为代码、说明、测试与本记录，
不包含这些远端权重及原图。

## 下次继续

1. 确认 Orin 使用最终版本，Inspect Square 同屏显示网格、预测框和置信度。
2. 用 rank4-12 测试 d4、d5、e4、e5、c5 等位置，记录真实类别与预测类别。
3. 测试单个白王在不同位置，以及两枚棋子真实相邻，检查重复候选和误删。
4. 若再出现重复检测，保留完整 JSON、对应无框输入图及 _pred.jpg，检查票数、深度和框位置。
5. 补齐白车/白后有效样本及其他未训练类别，留出独立位置和拍摄批次做验证。
