# HiveRobot handoff — 2026-09-12

## 今天完成的结果

1. 修正从已 capture 的 Table Clearance 到 Home 的边界检查。此前从实机当前姿态读取的 elbow 偏差可比预估的 `121.02°` 再大约 `0.5–1.3°`，导致 Home 在动作开始前拒绝执行。后续改为分阶段 Home 路径并按实际读数处理，不能简单把最大角度限制无限放宽。
2. 保留原有 Placement1，并在其后增加独立的 White Bishop Placement：从 Clearance 执行已训练的 `white_bishop_place` 到 c1。该 saved move 的七关节姿态训练完成，Claw Home 作为流程末尾的单独动作，不写入 saved move 本身。
3. 张爪时曾出现 wrist 下坠；改为在 Claw Home 期间继续保持 wrist 控制。15:44 回放张爪时 wrist 峰值偏移 `0.0219°`、结束偏移 `0°`，用户确认本轮表现良好。
4. 将 `white_bishop:d4`、`white_knight:c4` 抓取姿态和 `white_bishop:c1` 放置姿态登记为三条几何锚点。用户分别确认 D4 bishop 和 C4 knight 实际夹取。C4 的压力检测持续报告 `contact=false`，与目测结果不一致；按用户要求，Placement1 与 Placement_C4 均跳过“空夹”拦截，保留压力结果供诊断。
5. 新增 Placement_C4，从 C4 夹取后回到 Clearance，保留已有流程。21:34 回放 `returncode=0`，用户目测棋子夹住并停在 Clearance；该流程的 Clearance 七关节误差仍是 `training incomplete`。
6. 运行中的旧 Web 几何页不支持写入锚点，因此只更新 `chess_crown_geometry_v2_8.py` 一个模块。新 `/crown` 页可从本机选择几何 JSON、预览新增/更新/冲突，再写入机器人端。写入前自动备份机器人几何文件，且只合并锚点；不执行电机动作、不覆盖原有 `profiles`、`calibration` 或其他训练数据。用户预览到 `new=3, conflicts=0`，随后“读取已保存数据”返回三条锚点，证明机器人端写入成功。机器人上不要执行整库 `git pull`。

## 今天实机数据

| 动作 / 位置 | 验证与证据 | 尚需处理 |
|---|---|---|
| `white_bishop_place` → c1 | 14:53 完整流程 `training complete`；七关节最大残差 `0.4153°` | 放置 move 本身不含 Claw Home；流程末尾另行松爪 |
| c1 松爪 | 15:44 Claw Home 完成，wrist 峰值偏移 `0.0219°`、结束偏移 `0°`，用户确认正常 | 同次 Placement1 Clearance 的 `wrist_side` 残差 `2.4917°` |
| `bishop01` → d4 | 七关节姿态最大验证残差 `0.4809°`；用户确认成功夹取 | 棋盘到机械臂的三维坐标变换未验证 |
| `white_knight_c4` → c4 | 20:57 回放七关节最大残差 `0.4809°`；21:34 用户目测夹取成功 | 压力检测仍为 `contact=false`；抓取确认来自操作者观察 |
| Placement_C4 → Clearance | 21:34 回放 `returncode=0`，到达 Clearance 的视觉观察为成功，学习数据已保存 | `training incomplete`；0.5° 阈值下五关节超差 |

Placement_C4 最后一轮对 Clearance 的七关节残差（单位：度）：

| shoulder_front | shoulder_side | shoulder_rotate | elbow | arm_roll | wrist_side | wrist |
|---:|---:|---:|---:|---:|---:|---:|
| +0.9617 | +1.0491 | -4.6774 | +1.8360 | -0.4371 | +2.7977 | +0.1967 |

`white_bishop:c1` 的名义棋冠夹持中心是 `[137.5, 27.5, 55] mm`；`white_bishop:d4` 是 `[192.5, 192.5, 55] mm`；`white_knight:c4` 是 `[137.5, 192.5, 55] mm`。D4 和 C4 的棋冠夹持宽度均记为 `14 mm`。这些是示教锚点的名义棋盘位置，不是已验证的棋盘→机械臂三维变换。机器人返回的几何状态中 `profiles={}`、`calibration=null`、`motion_enabled=false`。

今天提供的 12 份 Web 命令粘贴日志及机器人锚点状态，原样归档在 `outputs/robot_runs_20260912/`，对应附件 ID 见该目录 README。仓库权威锚点 JSON 是 `scripts/data/chess_crown_geometry_v2_8.json`，包含三条锚点和验证/操作者确认元数据。

## 代码和数据位置

- `scripts/chessboard_vision_v2_8_web_control.py`：v2.8 Web 入口，已有功能和原流程继续保留。
- `scripts/left_arm_v2_8.py`、`scripts/left_arm_v2_8_move_library.py`：Home、replay、Placement1、Placement_C4、White Bishop Placement 的动作逻辑。
- `scripts/chess_crown_geometry_v2_8.py`：几何示教页与锚点安全导入接口。
- `scripts/data/chess_crown_geometry_v2_8.json`：三条本地几何锚点。
- `scripts/CHESS_CROWN_GEOMETRY_V2_8_MEASUREMENTS.md`：测量与锚点说明。
- `outputs/robot_runs_20260912/`：今天提供的 Web 输出和机器人端几何状态快照。

今天的主要代码提交：`ec9c87d`（Home 分阶段）、`05e7a1e`（追加 Bishop Placement）、`4b038e1`/`e8baada`（c1/d4 锚点）、`fea35e2`/`9f888bc`（放置末尾张爪并保持 wrist）、`2c8fdc8`/`56aacaf`（c4 姿态和尺寸）、`d49db65`（Placement_C4）、`6aa3239`（D4 夹取确认）、`c2b1f96`（几何页预览与安全写入）。完整提交历史以 `git log` 为准。

## 下次工作入口

1. 先从机器人单独备份 `DM_Control_Python/data/` 中的 saved moves、Clearance 和局部学习文件，再调整机械臂逻辑。机器人端这些权威运行数据没有随 Web JSON 状态返回，本次仅同步了用户提供的日志和几何锚点；不能宣称其余实机学习文件已备份到 GitHub，也不能以仓库副本覆盖它们。
2. 检查 C4 的“目测已夹住、压力接触为 false”现象。当前两个 Placement 流程按用户决定跳过空夹判断；压力值仍应记录，不能据此将传感接触标记为真。
3. 继续训练 Placement_C4 的 Clearance 残差，尤其是 `shoulder_rotate -4.6774°` 和 `wrist_side +2.7977°`。Placement1 的 `wrist_side` 仍需单独跟踪。`returncode=0` 或肉眼到位不等于 0.5° 训练完成。
4. 若要用 c1/d4/c4 锚点外推其他格，先取得更多分散示教点，校验关节正方向、工具中心、棋盘→机械臂坐标标定、逆运动学和避碰；当前几何页仍只提供不执行的坐标预览。
5. 若需再部署几何页，只替换对应 `.py` 文件，先备份机器人旧文件，重启原 Web 进程；通过 `/crown` 预览并合并 JSON。不要在机器人上整库 `git pull` 或覆盖 `data/`。

## 同步与验证

几何模块的 10 项单元测试和页面 JavaScript 语法检查于本日通过。今天先前代码已推送 `origin/main`；本 handoff、原始粘贴日志及说明修订在本次收尾提交中同步。最终提交号以提交后结果为准。
